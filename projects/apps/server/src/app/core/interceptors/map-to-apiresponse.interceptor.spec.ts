import { MapToAPIResponse } from './map-to-apiresponse.interceptor';

describe('MapToApiResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new MapToAPIResponse()).toBeDefined();
  });
});
